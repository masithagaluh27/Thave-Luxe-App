import 'package:flutter/material.dart';

class AppColors {
  // Define your aesthetic colors for better readability and consistency
  static const Color primaryGold = Color.fromARGB(
    255,
    180,
    154,
    129,
  ); // Muted gold/bronze
  static const Color darkBackground = Colors.black; // Deep black for background
  static const Color lightText = Color.fromARGB(
    255,
    255,
    254,
    250,
  ); // Off-white for main text
  static const Color cardBackground = Color.fromARGB(
    255,
    29,
    29,
    29,
  ); // Slightly lighter dark for cards
  static const Color subtleGrey =
      Colors.white54; // For hint texts, secondary icons etc.
  static const Color gradientDark = Color.fromARGB(
    255,
    35,
    35,
    35,
  ); // For gradient background (if used)
  static const Color imagePlaceholderBackground = Color(
    0xFFE0E0E0,
  ); 
  static const Color redAccent = Colors.redAccent; 
  static const Color green = Colors.green; 
  static const Color blue = Colors.blue; 
  static const Color backgroundLight = Color(
    0xFFF5F5F5,
  ); 
  static const Color backgroundGradientEnd = Color(
    0xFFE0E0E0,
  ); 
  static const Color cardBackgroundLight = Color(
    0xFFFFFFFF,
  ); 
  static const Color imagePlaceholderLight = Color(
    0xFFEFEFEF,
  ); 
  static const Color textDark = Color(
    0xFF1A1A1A,
  ); 
  static const Color subtleText = Color(
    0xFF616161,
  ); 
  static const Color searchBarBackground =
      Colors.white;
  static const Color searchBarBorder = Color(
    0xFFE0E0E0,
  ); 
  static const Color accentGrey = Color(
    0xFF757575,
  ); 
}
